{
  "name": "diamond-tip-tattoo",
  "private": true,
  "description": "Diamond Tip Tattoo website — static HTML + Vercel form webhooks & analytics",
  "scripts": {
    "build:analytics": "esbuild analytics-bootstrap.js --bundle --format=iife --platform=browser --outfile=vercel-analytics.js --define:process.env.NODE_ENV=\\\"production\\\"",
    "postinstall": "npm run build:analytics"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
